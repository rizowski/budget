type Goal {
  id: ID!
  name: String!
  currentAmount: Int!
  priority: Int!
  currentObjective: Objective!
  category: String!
  objectives: [ Objective ]!
}

type Objective {
  amount: Int!
  maxPerPaycheck: Int!
}

type GoalCategory {
  id: ID!
  name: String!
  priorities: [ Int! ]!
}
