type Goal {
  id: ID!
  name: String!
  amount: Int!
  priority: Int!
  objective: Objective!
  category: String!
  objectives: [ Objective ]!
}

type Objective {
  amount: Int!
  maxPerPaycheck: Int!
}

type GoalCategory {
  id: ID!
  name: String!
  priorities: [ Int! ]!
}
